apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
      targetPort: http-internal
      protocol: TCP
  selector:
    app: backend-service
